name: Poster Instagram

on:
  schedule:
    - cron: "*/15 * * * *"  # toutes les 15 minutes
  workflow_dispatch:         # pour déclencher manuellement si besoin

jobs:
  post_instagram:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write   # pour créer une issue GitHub en cas d'échec

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install requests

      - name: Process Instagram Payloads
        id: publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CYBERPUNK_ACCESS_TOKEN: ${{ secrets.CYBERPUNK_ACCESS_TOKEN }}
          CYBERPUNK_THREADS_TOKEN: ${{ secrets.CYBERPUNK_THREADS_TOKEN }}
          CYBERPUNK_BUFFER_API_KEY: ${{ secrets.CYBERPUNK_BUFFER_API_KEY }}
          GRACE_STORY_ACCESS_TOKEN: ${{ secrets.GRACE_STORY_ACCESS_TOKEN }}
          GRACE_STORY_THREADS_TOKEN: ${{ secrets.GRACE_STORY_THREADS_TOKEN }}
          GRACE_STORY_BUFFER_API_KEY: ${{ secrets.GRACE_STORY_BUFFER_API_KEY }}
          INFLUENCER_ACCESS_TOKEN: ${{ secrets.INFLUENCER_ACCESS_TOKEN }}
          INFLUENCER_THREADS_TOKEN: ${{ secrets.INFLUENCER_THREADS_TOKEN }}
          INFLUENCER_BUFFER_API_KEY: ${{ secrets.INFLUENCER_BUFFER_API_KEY }}
          SUNDRESS_ACCESS_TOKEN: ${{ secrets.SUNDRESS_ACCESS_TOKEN }}
          SUNDRESS_THREADS_TOKEN: ${{ secrets.SUNDRESS_THREADS_TOKEN }}
          SUNDRESS_BUFFER_API_KEY: ${{ secrets.SUNDRESS_BUFFER_API_KEY }}
          UNIVERSITY_ACCESS_TOKEN: ${{ secrets.UNIVERSITY_ACCESS_TOKEN }}
          UNIVERSITY_THREADS_TOKEN: ${{ secrets.UNIVERSITY_THREADS_TOKEN }}
          UNIVERSITY_BUFFER_API_KEY: ${{ secrets.UNIVERSITY_BUFFER_API_KEY }}
          CYBERPUNK_FACEBOOK_ID: ${{ vars.CYBERPUNK_FACEBOOK_ID }}
          CYBERPUNK_INSTAGRAM_ID: ${{ vars.CYBERPUNK_INSTAGRAM_ID }}
          CYBERPUNK_THREADS_ID: ${{ vars.CYBERPUNK_THREADS_ID }}
          CYBERPUNK_THREADS_TOKEN_EXPIRES: ${{ vars.CYBERPUNK_THREADS_TOKEN_EXPIRES }}
          CYBERPUNK_BUFFER_TIKTOK_CHANNEL_ID: ${{ vars.CYBERPUNK_BUFFER_TIKTOK_CHANNEL_ID }}
          CYBERPUNK_BUFFER_YOUTUBE_CHANNEL_ID: ${{ vars.CYBERPUNK_BUFFER_YOUTUBE_CHANNEL_ID }}
          GRACE_STORY_FACEBOOK_ID: ${{ vars.GRACE_STORY_FACEBOOK_ID }}
          GRACE_STORY_INSTAGRAM_ID: ${{ vars.GRACE_STORY_INSTAGRAM_ID }}
          GRACE_STORY_THREADS_ID: ${{ vars.GRACE_STORY_THREADS_ID }}
          GRACE_STORY_THREADS_TOKEN_EXPIRES: ${{ vars.GRACE_STORY_THREADS_TOKEN_EXPIRES }}
          GRACE_STORY_BUFFER_TIKTOK_CHANNEL_ID: ${{ vars.GRACE_STORY_BUFFER_TIKTOK_CHANNEL_ID }}
          GRACE_STORY_BUFFER_YOUTUBE_CHANNEL_ID: ${{ vars.GRACE_STORY_BUFFER_YOUTUBE_CHANNEL_ID }}
          INFLUENCER_FACEBOOK_ID: ${{ vars.INFLUENCER_FACEBOOK_ID }}
          INFLUENCER_INSTAGRAM_ID: ${{ vars.INFLUENCER_INSTAGRAM_ID }}
          INFLUENCER_THREADS_ID: ${{ vars.INFLUENCER_THREADS_ID }}
          INFLUENCER_THREADS_TOKEN_EXPIRES: ${{ vars.INFLUENCER_THREADS_TOKEN_EXPIRES }}
          INFLUENCER_BUFFER_TIKTOK_CHANNEL_ID: ${{ vars.INFLUENCER_BUFFER_TIKTOK_CHANNEL_ID }}
          INFLUENCER_BUFFER_YOUTUBE_CHANNEL_ID: ${{ vars.INFLUENCER_BUFFER_YOUTUBE_CHANNEL_ID }}
          SUNDRESS_FACEBOOK_ID: ${{ vars.SUNDRESS_FACEBOOK_ID }}
          SUNDRESS_INSTAGRAM_ID: ${{ vars.SUNDRESS_INSTAGRAM_ID }}
          SUNDRESS_THREADS_ID: ${{ vars.SUNDRESS_THREADS_ID }}
          SUNDRESS_THREADS_TOKEN_EXPIRES: ${{ vars.SUNDRESS_THREADS_TOKEN_EXPIRES }}
          SUNDRESS_BUFFER_TIKTOK_CHANNEL_ID: ${{ vars.SUNDRESS_BUFFER_TIKTOK_CHANNEL_ID }}
          SUNDRESS_BUFFER_YOUTUBE_CHANNEL_ID: ${{ vars.SUNDRESS_BUFFER_YOUTUBE_CHANNEL_ID }}
          UNIVERSITY_FACEBOOK_ID: ${{ vars.UNIVERSITY_FACEBOOK_ID }}
          UNIVERSITY_INSTAGRAM_ID: ${{ vars.UNIVERSITY_INSTAGRAM_ID }}
          UNIVERSITY_THREADS_ID: ${{ vars.UNIVERSITY_THREADS_ID }}
          UNIVERSITY_THREADS_TOKEN_EXPIRES: ${{ vars.UNIVERSITY_THREADS_TOKEN_EXPIRES }}
          UNIVERSITY_BUFFER_TIKTOK_CHANNEL_ID: ${{ vars.UNIVERSITY_BUFFER_TIKTOK_CHANNEL_ID }}
          UNIVERSITY_BUFFER_YOUTUBE_CHANNEL_ID: ${{ vars.UNIVERSITY_BUFFER_YOUTUBE_CHANNEL_ID }}

        run: |
          echo "[REFRESH] Lancement du script de publication Instagram..."
          python scripts/process_payloads.py

      - name: Notify on failure
        if: failure() && steps.publish.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            const now = new Date().toLocaleString('fr-CH', {timeZone: 'Europe/Zurich'});
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              title: `⚠️ Publication Instagram échouée — ${now}`,
              body:  `Des erreurs ont été détectées lors de la publication automatique.\n\n` +
                     `👉 [Voir les logs](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`,
              labels: []
            });
